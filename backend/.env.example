# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=chyrralon
DB_USER=
DB_PASSWORD=

# Application Configuration
APP_ENV=development
APP_DEBUG=true

# API Configuration
API_VERSION=v1

# Base path for subdirectory deployments (e.g. /chyrralon)
APP_BASE_PATH=/chyrralon

# Logging
LOG_LEVEL=debug

# WebHatchery Auth (shared JWT)
JWT_SECRET=your_webhatchery_jwt_secret
